import React, { Component } from 'react';

/**
 *
 * Simple component for showing "Loading..." text when needed
 *
 */
export default class Loading extends Component {
    render() {
        return (
            <p>Loading...</p>
        );
    }
}